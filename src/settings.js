import pkg from "@/../package.json";

let globalSettings = {
    // project_name:pkg.name,
    project_name:'数据智能分析平台',
    // localStorage sessionStorage 前缀
    storagePrefix: 'sjznfx_',
}


export default globalSettings
